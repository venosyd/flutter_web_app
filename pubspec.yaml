name: flutter_web_app
description: Easy WebView App based on Flutter
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.2.1+3

environment:
  sdk: ">=2.10.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_webview_plugin: 

dev_dependencies:
  flutter_test:
    sdk: flutter
